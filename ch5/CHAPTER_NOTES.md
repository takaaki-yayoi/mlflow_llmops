# 第5章 リスト ↔ リポジトリ対応表 と 既知の挙動差分

本書 第5章「改善サイクルを加速する―― 評価の仕組み」の各リスト (コード片) と、本リポジトリの実装の対応表です。本書は紙面の都合で動作環境設定や周辺コードを省略しており、ここではリポジトリと本書の差分のうち、**読者が混乱しやすい点・実行結果が本書と異なる点**に絞って解説します。

リポジトリのコードは可能な限り本書本文に寄せるように整備されています。残った差分は、リポジトリの動作上やむを得ないもの (UI操作前提のコードを自動化スクリプトに置き換えたもの等) と、本書側に明らかな矛盾があり寄せると害があるもの (errata候補) のいずれかです。

# 5.2 評価の始め方: まずは実際に動かしてみる

## リスト5.1 質問を設定し、回答を生成

- **対応ファイル**: `evaluation/01_vibe_check.py`
- **実行**: `make vibe-check`
- **差分**: 質問文・実行ロジックは本書通り。リポジトリ側はエラーハンドリングと整形出力が追加されている。

# 5.4 評価基準を決定する

## リスト5.2 「ツール使用の適切性」を評価
## リスト5.3 「情報の正確さ」を評価

- **対応ファイル**: `evaluation/02_standard_scorers.py`
- **実行**: `make test-standard`
- **本書が採点対象にしているトレース**: 本書 5.2節のテスト質問のうち、**質問2「LangGraphエージェントのトークン使用量を追跡するにはどうすれば良いですか？」** のトレースです。リスト5.1の並び順は以下のとおりで、質問1は「実験管理はどのように始めれば良いですか？」です。

  | 番号 | 質問 |
  | --- | --- |
  | 質問1 | 実験管理はどのように始めれば良いですか？ |
  | 質問2 | LangGraphエージェントのトークン使用量を追跡するにはどうすれば良いですか？ ← リスト5.2/5.3 はこのトレースを使う |
  | 質問3 | MLflowトレーシングはどのフレームワークに対応していますか？ |

- **残る差分**:
  * 本書では「MLflow UIで質問2のトレースIDをコピーして `mlflow.get_trace("<ID>")` で取得する」流れで説明していますが、本リポジトリではUI操作なしで実行できるように `get_latest_traces()` で最新トレースを取得し、その中から**質問文で質問2のトレースを検索**して採点します (`TARGET_QUESTION` / `select_target_trace()`) 。
  * 本書のリスト5.2の判定理由 `Missing: {'doc_search'}; Unexpected: {'web_search'}` は、質問2のトレースで web_search が使われた場合の例です。エージェントがどのツールを選ぶかは実行ごとに変わるため、doc_search が使われた実行では `yes` と判定されます。
  * 質問2のトレースが直近5件に見つからない場合は最新トレースにフォールバックし、その旨を実行時に表示します。この場合はリスト5.3の正解データと質問が噛み合わないため Correctness が `no` になります。`make vibe-check` を実行し直してから `make test-standard` を実行してください。
- **アライメント済み**: リスト5.3の `expected_response` は本書と同じ長文 (LangGraphトークン使用量に関する内容) に揃えました。

### 出力される `rationale` について

`make test-standard` を実行すると、`value` と並んで英語の長文 `rationale` が出力されます。読み解くうえでのポイントは以下のとおりです。

- `value` は集計に使われる判定値 (`yes` / `no` など) 、`rationale` はその判定を下した**理由**です。本書リスト5.2の `判定理由: Missing: {'doc_search'}; Unexpected: {'web_search'}` がこれにあたります。
- `rationale` が入るのは組み込みスコアラーと `make_judge` / `Guidelines` で作ったLLMジャッジです。`@scorer` デコレータで書いたルールベースのカスタムスコアラー (例: `contains_code_block`) では `None` になります。
- LLMジャッジ (`Correctness`、`Guidelines`、`make_judge`) の `rationale` はLLMが生成した自然文です。一方 `ToolCallCorrectness` に `expected_tool_calls` を渡した場合はツールリストの決定的な比較になるため、`Missing: {'doc_search'}; Unexpected: {'web_search'}` のような機械的な差分が入ります。
- 英語で出力されるのは、組み込みジャッジのプロンプトが英語で固定されているためです。日本語の判定理由が欲しい場合は `make_judge` の `instructions` を日本語で書きます (`scorers.py` の `katakana_judge` がその例) 。
- `no` になった原因を切り分ける一次情報になります。スコアラーの不具合なのか、採点対象のトレースと正解データが噛み合っていないのかは、`rationale` を読むと判別できます。
- 5.8節のLLMジャッジのアライメントでは、この `rationale` を人間が読んでジャッジの妥当性を検証し、フィードバックを与える流れになります。

## リスト5.4 「サンプルコードの生成」を評価するスコアラー
## リスト5.5 「サンプルコードの生成」を評価

- **対応ファイル**: スコアラー定義は `evaluation/scorers.py`、実行は `evaluation/03_custom_scorers.py`
- **実行**: `make test-custom`
- **差分**: リスト5.5は本書では独立スニペット形ですが、リポジトリでは `03_custom_scorers.py` の中に組み込まれています。スコアラー本体のコードは本書通り。

## リスト5.6 「参照リンクの提示」を評価するスコアラー
## リスト5.7 「参照リンクの提示」を評価

- **対応ファイル**: スコアラー定義は `evaluation/scorers.py`、実行は `evaluation/03_custom_scorers.py`
- **アライメント済み**: スコアラー名・guidelines文字列を本書と一字一句揃えました (「ユーザーの質問」の長音記号など) 。

## リスト5.8 make_judgeを使ったスコアラー

- **対応ファイル**: `evaluation/scorers.py` の `katakana_judge`
- **残る差分**:
  * 本書では `make_judge(name=..., instructions=..., feedback_value_type=...)` でデフォルトモデル前提。
  * リポジトリでは実行可能性のため `model="openai:/gpt-4o-mini"` を**明示的に指定**しています。
  * **OpenAI 以外の API キーしかない場合**は、`scorers.py` の `katakana_judge` の `model` 引数を該当プロバイダ (例: `anthropic:/claude-sonnet-4-20250514`、`google:/gemini-2.0-flash`) に変更してください。
- **アライメント済み**: instructions本文の表記 (「等」→「など」、「方が」→「ほうが」) を本書通りに揃えました。

## リスト5.9 登録済みのスコアラーを利用

- **対応ファイル**: `evaluation/04_register_scorers.py`
- **実行**: `make register`
- **アライメント済み**: 本書通りに `from mlflow.genai import get_scorer` で直接インポートし、`version=1` を明示する形に揃えました。
- **本書側のerrata候補**: 本書のコメントは「最新バージョンを取得」となっているにもかかわらず `version=1` を明示しています。最新バージョンを取得したい場合は `version` 引数を省略してください。

# 5.5 評価データセットの準備

## リスト5.10 評価データセットの準備

- **対応ファイル**: `evaluation/05_run_evaluation.py` の `eval_dataset`
- **差分**: 本書ではデータセットの1件目のみ抜粋し、残り4件は「サンプルコードを参照」となっています。完全な5件の定義は `05_run_evaluation.py` を確認してください。
- **アライメント済み**: 質問2 (LangGraphエージェントのトークン使用量) の `expected_response` を本書 リスト5.3 の長文と完全一致させました。

# 5.6 自動評価の実行

## リスト5.11 評価スクリプトの全体構成
## リスト5.12 predict_fn の定義
## リスト5.13 評価の実行

- **対応ファイル**: `evaluation/05_run_evaluation.py`
- **実行**: `make eval`
- **差分**: 本書は構成を示す抜粋。実行可能な完全版を `05_run_evaluation.py` で確認してください。

# 5.7 応用 (1): 会話セッションのシミュレーションと評価

> 本書5.7節冒頭で、応用編 (5.7-5.9節) は読み飛ばし可能であることが明記されています。

## リスト5.14 会話セッション評価のためのカスタムスコアラー (LLMジャッジ)

- **対応ファイル**: なし (本書での例示のみ)
- **補足**: 本書のリスト5.14 は make_judge による会話セッション評価のスコアラー定義例ですが、本リポジトリの `06_conversation_sim.py` の評価では使用していません。`06_conversation_sim.py` では組み込みの `ConversationCompleteness` と `UserFrustration` を使用しています。

## リスト5.15 会話のシミュレートにおける目標とペルソナ
## リスト5.16 会話シミュレーションにおける predict_fn
## リスト5.17 会話の生成と評価

- **対応ファイル**: `evaluation/06_conversation_sim.py`
- **実行**: `make sim`
- **差分**: 本書は構成を示す抜粋。実行可能な完全版は `06_conversation_sim.py` を参照してください。
- **コスト注意**: 会話生成に追加のLLM呼び出しが発生するため、APIコストに注意してください。

## リスト5.18 既存の会話セッションから目標とペルソナを抽出

- **対応ファイル**: 本リポジトリ未収録
- **理由**: 本書のNoticeにあるとおり `mlflow.search_sessions` および `generate_test_cases` は **MLflow 3.10 以降の Experimental API** であり、本書執筆時点の安定版 (MLflow 3.9.0) では使用できません。
- **試したい場合**: `pip install mlflow>=3.10` でアップグレードしたうえで、本書のリスト5.18のコードをそのまま試してください。Experimental APIのため仕様変更の可能性があります。

# 5.7.6 テストケースのデータセット管理

## リスト5.19 テストケースの保存と利用

- **対応ファイル**: 本リポジトリ未収録
- **理由**: 単発の API 例示のため。
- **試したい場合**: 本書のリスト5.19 のコードを `evaluation/06_conversation_sim.py` の前段に挿入することで動作します。

# 5.8 応用 (2): LLMジャッジのアライメント

## リスト5.20-5.22 アライメントの実行

- **対応ファイル**: 本リポジトリ未収録
- **理由**: アライメントには事前に「LLMジャッジでの評価結果に対して人間がレビューしたフィードバック」がトレースに付与されている必要があり、これを本リポジトリ単体で再現するには手作業が伴うためサンプル化していません。
- **試したい場合**:
  1. `make eval` で評価を実行
  2. MLflow UI でトレースを開き、Assessmentタブから各LLMジャッジ判定に対して人間のフィードバックを記録
  3. その後、本書リスト5.20 のコードを実行
- **補足**: MemAlign は MLflow 3.9.0 以降で利用可能です。本書のNoticeを参照してください。

# 5.9 応用 (3): サードパーティ評価ライブラリとの連携

## リスト5.23 サードパーティのスコアラーを利用 (RAGAS / DeepEval)

- **対応ファイル**: 本リポジトリ未収録
- **理由**: DeepEval や RAGAS は MLflow とは別パッケージのため、リポジトリの依存関係に含めていません。
- **試したい場合**:

```
uv add deepeval ragas
```

  でインストール後、本書のリスト5.23 のコードを `evaluation/05_run_evaluation.py` の `scorers=[...]` に追加してください。

- **MLflow バージョン**: `mlflow.genai.scorers.deepeval` および `mlflow.genai.scorers.ragas` への組み込みは **MLflow 3.8.0 以降** で利用可能です (本書記載どおり)。

# 全体的な注意事項

- 本書本文のコード片は「読んで理解するため」の抜粋であり、実行可能な完全版は本リポジトリにあります。MLflow のバージョンアップに追従するため、リポジトリ側を継続的に更新しています。
- リポジトリのコードは原稿との突合作業を経て、可能な限り本書本文に寄せています。残った差分は本ドキュメントに明記しています。
- 本ドキュメントに未記載の挙動差分や実装上の不整合を発見された場合は、GitHub Issues で `errata` ラベルを付けて報告いただければ随時更新します。
