# QAエージェント Kubernetes Service（7.5.3節）
apiVersion: v1
kind: Service
metadata:
  name: qa-agent-service
  labels:
    app: qa-agent
spec:
  selector:
    app: qa-agent
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
